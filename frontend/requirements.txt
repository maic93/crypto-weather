# Minimal dependencies for Vercel serverless functions
# Heavy ML packages (pandas, numpy, scikit-learn) are NOT included
# Forecasts are pre-computed and stored in the DB by the daily cron
fastapi==0.115.0
mangum==0.17.0
httpx==0.27.0
sqlalchemy==2.0.36
pg8000==1.31.2
pydantic==2.11.7
pydantic-settings==2.6.0
python-dotenv==1.0.1
